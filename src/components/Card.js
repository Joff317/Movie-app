import React from "react";
import { API_IMAGE } from "../config";

const Card = ({ movie }) => {
  return (
    <div className="card">
      <img src="" alt="" />
    </div>
  );
};

export default Card;
